name: CodeQL
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  analyze:
    name: Analyze
    uses: github/codeql-action/analyze@v2
    with:
      languages: ['python', 'csharp']
